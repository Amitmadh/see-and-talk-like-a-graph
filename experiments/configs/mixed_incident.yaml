# Mixed Signals with the count-oriented incident encoding.
# All 7 GraphQA tasks. Exp2 skips a task whose corrupt-text JSONL exists.
# Requires the incident clean baseline first:
#   results/baseline/text_and_image/<task>/spring_incident_Qwen2.5-VL-7B-Instruct.jsonl

data_dir: data

tasks:
  - node_degree
  - node_count
  - edge_existence
  - edge_count
  - disconnected_nodes
  - triangle_counting
  - cycle_check

model: qwen-7b

modalities:
  - text_and_image

text_encoding: "incident"

image_types:
  - spring

batch_size: 1

output_dir: results/mixed_baseline/
