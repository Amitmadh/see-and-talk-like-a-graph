# baseline.yaml

data_dir: data

tasks:
   - connected_nodes
   - shortest_path
   - node_degree
   - node_count
   - edge_existence
   - edge_count
   - disconnected_nodes
   - triangle_counting
   - cycle_check


model: qwen-7b
   #Qwen2.5-VL


modalities:
  - text_and_image

text_encoding: "adjacency"

image_types:
  - spring

batch_size: 1

output_dir: results/mixed_baseline/
